<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.fansfunding.user.dao.UserMomentDao" >
  <resultMap id="BaseResultMap" type="com.fansfunding.user.entity.UserMoment" >
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="user_id" property="userId" jdbcType="INTEGER" />
    <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
    <result column="del_flag" property="delFlag" jdbcType="CHAR" />
    <result column="content" property="content" jdbcType="LONGVARCHAR" />
  </resultMap>
  
  <update id="deleteByPrimaryKey" parameterType="java.lang.Integer" >

  </update>
  
  <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="com.fansfunding.user.entity.UserMoment" >
	insert into user_moments(user_id,content)
		values(#{userId},#{content})
  </insert>
  
  <update id="updateByPrimaryKey" parameterType="com.fansfunding.user.entity.UserMoment" >

  </update>
  
  <select id="selectFriendsByUserId" resultMap="BaseResultMap" parameterType="java.lang.Integer" >
	select umoments.id, umoments.user_id, umoments.create_time, umoments.del_flag, umoments.content
		from user_moments umoments
		where umoments.user_id in (
			select fuser.user_id 
			from follow_user fuser 
			where fuser.follower_id=#{userId,jdbcType=INTEGER}
			and del_flag !=1
		)
		and del_flag !=1
		order by umoments.create_time desc
  </select>
  
  <select id="selectByUserId" resultMap="BaseResultMap" parameterType="java.lang.Integer" >
		select id, user_id, create_time, del_flag, content
			from user_moments
			where user_id=#{userId,jdbcType=INTEGER}
			and del_flag !=1
			order by create_time desc
  </select>
  
  <select id="selectById" resultMap="BaseResultMap" parameterType="java.lang.Integer" >
		select id, user_id, create_time, del_flag, content
			from user_moments
			where id=#{id,jdbcType=INTEGER}
			and del_flag !=1
  </select>
</mapper>